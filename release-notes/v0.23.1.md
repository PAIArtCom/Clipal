## v0.23.1

### English

#### Fixed

- Import plans no longer become stale because routine provider usage is recorded or an OAuth token refreshes while the user reviews the preview.
- Import still rejects Apply when configuration or the credential account set changes after preview.

---

### 中文

#### 修复

- 用户审查导入预览期间，即使产生常规 Provider 用量或 OAuth token 刷新，导入计划也不会被错误标记为过期。
- 若预览后配置或凭据账号集合发生变化，导入仍会拒绝 Apply。
